fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pandas==2.2.2
numpy==1.26.4
python-multipart==0.0.9
python-dotenv==1.0.1
streamlit==1.38.0
requests==2.32.3
pytest==8.3.2

# Optional — needed only for the corresponding document type.
pdfplumber==0.11.4      # PDF invoice extraction
pytesseract==0.3.13     # image/OCR invoice extraction (also requires the tesseract binary)
Pillow==10.4.0          # image handling for OCR
anthropic==0.34.2       # LLM-based audit summaries; system works without it (template fallback)
